To run the code, you need to have Node.js and RabbitMQ installed on your machine.

Here are the steps to run the code:

Clone the repository and navigate to the root directory.
Install the dependencies by running npm install.
Start the RabbitMQ server.
Run the index.js file using node index.js.
You should see the messages being sent and received in the console.