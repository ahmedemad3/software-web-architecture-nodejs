To run the code, follow these steps:

Install the ioredis package by running npm install ioredis.
Start a Redis server on your local machine by running redis-server in your terminal.
Open two terminal windows and run node producer.js in one and node consumer.js in the other.